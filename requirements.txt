# O.L.I.V.I.A. runtime dependencies — Windows + NVIDIA CUDA 12.6
# NOT portable to Linux/CI as-is (+cu126 wheels come from the PyTorch index below).
# CI installs `.[dev]` from pyproject.toml instead.
# requirements.lock holds the full frozen transitive set for exact reproduction.
#
# Install (two steps):
#   pip install -r requirements.txt
#   pip install chatterbox-tts==0.1.6 --no-deps
#
# chatterbox-tts hard-pins torch==2.6.0, which conflicts with this project's
# torch 2.10/cu126 stack (it runs fine on it). Its runtime dependencies are
# declared explicitly in the "ChatterBox dependencies" section below, so the
# package itself must be installed with --no-deps. `pip check` will therefore
# report chatterbox-tts complaints (torch/transformers versions; gradio and
# other demo-only extras "missing") — these are known and harmless.
--extra-index-url https://download.pytorch.org/whl/cu126

# GPU stack
torch==2.10.0+cu126
torchaudio==2.10.0+cu126

# Core LLM
ollama==0.6.1

# Configuration
python-dotenv==1.2.1
PyYAML==6.0.3

# Memory system
chromadb==1.4.1
sentence-transformers==5.2.0

# TTS — ChatterBox dependencies (chatterbox-tts itself installed separately, see header)
librosa==0.11.0
s3tokenizer==0.3.0
transformers==4.57.6
diffusers==0.29.0
resemble-perth==1.0.1
conformer==0.3.2
safetensors==0.5.3
omegaconf==2.3.0
sounddevice==0.5.3
soundfile==0.13.1

# STT + VAD
faster-whisper==1.2.1
# av 18.x DLLs are blocked by Windows Application Control on this machine
av==16.1.0
silero-vad==5.1.2

# Audio processing
numpy==1.25.2
scipy==1.16.3

# Web search
requests==2.32.5
beautifulsoup4==4.14.3
trafilatura==2.0.0

# FastAPI backend
fastapi==0.128.0
starlette==0.50.0
uvicorn[standard]==0.40.0
pydantic==2.11.10
pydantic-settings==2.12.0
websockets==15.0.1
python-multipart==0.0.21

# Flet desktop UI
flet==0.80.3
httpx==0.28.1
